/**
 * Build-time stub. In a dev checkout this stays empty (assets come from disk);
 * the packaging script swaps this module for one carrying the stylesheet and
 * fonts, so the single-file executable serves them from memory.
 */
export const EMBEDDED = null;
